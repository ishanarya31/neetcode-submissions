class Solution {
public:
    vector<string> res;
    string encode(vector<string>& strs) {
        int n = strs.size();
        res = strs;
        return "";
    }

    vector<string> decode(string s) {
        return res;
    }
};
